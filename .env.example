# Paper Scope environment configuration
#
# Copy this file to `.env` in the repository root and replace the placeholder
# values with secrets for your environment. The FastAPI backend loads these
# settings via pydantic-settings when started through `make backend-dev` or the
# container stack.

# Required for features that call OpenAI models. Comment out if you do not plan
# to use LLM-powered enrichment.
PAPER_SCOPE_OPENAI_API_KEY=sk-your-openai-key

# Optional: override the timezone used by the background scheduler. Defaults to
# UTC when unset.
# PAPER_SCOPE_SCHEDULER_TIMEZONE=UTC
