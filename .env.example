# Paper Scope environment configuration

PAPER_SCOPE_APP_NAME=Paper Scope API
PAPER_SCOPE_STORAGE_ROOT=/data/papers
PAPER_SCOPE_LLM_PROVIDER=mock
# Set when using OpenAI enrichment
PAPER_SCOPE_OPENAI_API_KEY=
PAPER_SCOPE_OPENAI_MODEL=gpt-4o-mini
# Configure Ollama when using local models
# When backend runs in containers, target the host bridge.
# Override with http://localhost:11434 when running locally.
PAPER_SCOPE_OLLAMA_BASE_URL=http://host.containers.internal:11434
PAPER_SCOPE_OLLAMA_MODEL=mistral
PAPER_SCOPE_NEO4J_URI=bolt://neo4j:7687
PAPER_SCOPE_NEO4J_USER=neo4j
# Minimum 8 characters as required by Neo4j.
PAPER_SCOPE_NEO4J_PASSWORD=changeme123
PAPER_SCOPE_SCHEDULER_TIMEZONE=UTC
# Frontend service talks to backend via this base URL.
# Use http://localhost:8000/api when running both apps locally.
PAPER_SCOPE_BACKEND_URL=http://backend:8000/api
# Public URL (used by the browser) to reach the backend API.
# Defaults to /api so it works behind nginx; override to http://localhost:8080/api when accessing directly.
PAPER_SCOPE_BACKEND_PUBLIC_URL=/api
